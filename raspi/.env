# WebSocket URI for the FastAPI hub.
# Change the IP to match your laptop's address on the hackathon network.
# The Pi registers itself as client_id "pi", so the path must end with /ws/pi.
FASTAPI_WS_URI=ws://192.168.2.1:8000/ws/pi
